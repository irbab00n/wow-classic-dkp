import * as blizzardController from '../controllers/blizzard';

const routes = [
  {
    verb: 'get',
    endpoint: '/blizzard/character',
    controller: () => {}, // ! stubbed with an anon function for now
  },
];

export default routes;
